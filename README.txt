After entering this directory run the make command in the terminal. This will compile both scheduler and simulator programs.
Inorder to run the scheduler enter the ./scheduler command in the terminal and inorder to run the simulator enter the ./simulator command
in the terminal.